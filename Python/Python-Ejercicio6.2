# Crear un programa que tenga una clase llamada Alumno que tenga como atributos su nombre y su nota.
# Deberéis de definir los métodos para inicializar sus atributos, imprimirlos y mostrar un mensaje con el resultado de la nota y si ha aprobado o no.

class Alumno:

    nombre = ""
    nota = 0
    pasa = "aprobado"

    def __init__(self, nombre, nota):
        self.nota = nota
        self.nombre = nombre
        if nota < 5:
            self.pasa = "suspendido"

    def __str__(self):
        return f"{self.nombre} ha sacado una nota de {self.nota} y está {self.pasa}"


alumno1 = Alumno("Pablo", 6.5)
print(alumno1)
